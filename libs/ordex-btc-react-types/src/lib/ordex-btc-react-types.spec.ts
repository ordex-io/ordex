import { ordexBtcReactTypes } from './ordex-btc-react-types'

describe('ordexBtcReactTypes', () => {
  it('should work', () => {
    expect(ordexBtcReactTypes()).toEqual('ordex-btc-react-types')
  })
})
